# Checkouts of the three libraries the example applications build against, cloned here so that the
# relative paths in each Makefile.
/fact-graph/
/form-builder/
/taxpert/


# Generated mirrors of taxpert's packages/ui/src, written by `make copy-shared-ui` (a prerequisite
# of every build target here). Kept out of git so they can
# never drift from the taxpert source they mirror. `make check-shared-ui` fails the build if they do.
credit-assistant/src/main/resources/credit-assistant/website-static/vendor/taxpert/
tax-withholding-estimator/src/main/resources/twe/website-static/vendor/taxpert/
benefits-enrollment/src/main/resources/benefits-enrollment/website-static/vendor/taxpert/

# Scala.js sourcemaps vendored by `make copy-fg`
**/vendor/fact-graph/*.map

.env
out/


target/
project/target/
.bsp/
.bloop/
.metals/
.scala-build
metals.sbt

node_modules/

.idea/
*.iml
.vscode/*
!.vscode/extensions.json
!.vscode/fg-helpers.code-snippets

.DS_Store
