<!-- GENERATED by `make transpile`. Do not edit: change codemod/components.ts and regenerate. -->

# Component coverage

Every content component type Direct File's flow declares, and what this port does with it.
Written from the same pass that emits the flow, over 727 screens and 4346 content declarations.

An unmapped type is not a row here — `ComponentMapper.blocks` throws on it, naming the type and the screen, and the emit fails. So this file is a list of decisions, and every one of them is in `codemod/components.ts`.

| Group | Types |
|---|---:|
| Expressed in the flow | 34 |
| Drawn by something else | 3 |
| Out of scope | 13 |
| Real gaps | 1 |
| **Total** | **51** |

## Expressed in the flow

Each becomes flow markup. Where a type has more than one row, its props decide: content marked `displayOnlyOn: 'data-view'` never appears on an editing screen, and a topic page is an editing screen.

| Component | Declarations | Becomes |
|---|---:|---|
| `Address` | 8 | `<fg-set>` with `<input type="address">` |
|  | 1 | skipped where displayOnlyOn is data-view |
| `BankAccount` | 2 | `<fg-set>` with `<input type="bank-account">` |
| `BigContent` | 6 | the key's content: a body tree, a sentence, or a modal launcher |
| `Boolean` | 244 | `<fg-set>` with `<input type="boolean">` |
|  | 2 | skipped where displayOnlyOn is data-view |
| `ConditionalAccordion` | 7 | `<fg-detail>` |
| `ConditionalList` | 58 | `<ul>`, one `<li>` per item that still has a condition |
| `ContextHeading` | 225 | `<p class="df-context-heading">` |
|  | 1 | skipped where displayOnlyOn is data-view |
| `DatePicker` | 10 | `<fg-set>` with `<input type="date">` |
|  | 2 | skipped where displayOnlyOn is data-view |
|  | 1 | read-only: a `<p>` with `<fg-show>`, not a question |
| `DFAccordion` | 27 | `<fg-detail>` |
| `DFAlert` | 181 | `<fg-alert>` |
| `DFModal` | 471 | `<modal-link>` plus a hoisted `<modal-dialog>` |
| `Dollar` | 120 | `<fg-set>` with `<input type="dollar">` |
|  | 26 | skipped where displayOnlyOn is data-view |
|  | 12 | read-only: a `<p>` with `<fg-show>`, not a question |
| `Ein` | 6 | `<fg-set>` with `<input type="ein">` |
| `Enum` | 61 | `<fg-set>` with `<input type="enum">` |
| `FactAssertion` | 4 | skipped where displayOnlyOn is data-view |
| `FactResultAssertion` | 2 | skipped where displayOnlyOn is data-view |
| `FactSelect` | 4 | `<fg-set>` with `<input type="select">` |
|  | 2 | skipped where displayOnlyOn is data-view |
| `GenericString` | 14 | skipped where displayOnlyOn is data-view |
|  | 4 | `<fg-set>` with `<input type="text">` |
|  | 2 | read-only: a `<p>` with `<fg-show>`, not a question |
| `Heading` | 956 | the screen's `<h2>`, and the page title when the screen opens one |
| `HelpLink` | 12 | the key's content: a body tree, a sentence, or a modal launcher |
| `Hint` | 5 | `<p class="usa-hint">` |
| `IconDisplay` | 120 | dropped: decorative |
| `IconList` | 4 | `<ul>`, one `<li>` per line |
| `InfoDisplay` | 699 | the key's content: a body tree, a sentence, or a modal launcher |
| `InternalLink` | 31 | `<p>` with a link to the named flow route |
| `IntroContent` | 15 | the key's content: a body tree, a sentence, or a modal launcher |
| `IpPin` | 6 | `<fg-set>` with `<input type="ip-pin">` |
|  | 1 | read-only: a `<p>` with `<fg-show>`, not a question |
| `LimitingString` | 44 | `<fg-set>` with `<input type="text">` |
|  | 12 | read-only: a `<p>` with `<fg-show>`, not a question |
|  | 4 | skipped where displayOnlyOn is data-view |
| `MultiEnum` | 6 | `<fg-set>` with `<input type="multi-enum">` |
| `PhoneNumber` | 4 | `<fg-set>` with `<input type="phone-number">` |
| `Pin` | 6 | `<fg-set>` with `<input type="pin">` |
| `Subheading` | 14 | `<h3>` |
| `SummaryTable` | 22 | `<table>` of label/value rows |
| `Tin` | 12 | `<fg-set>` with `<input type="tin">` |
|  | 5 | read-only: a `<p>` with `<fg-show>`, not a question |
|  | 1 | skipped where displayOnlyOn is data-view |

## Drawn by something else

Present upstream as a component, present here as part of the page or the collection it sits in.

| Component | Declarations | Becomes |
|---|---:|---|
| `CollectionItemManager` | 12 | `<fg-collection>` draws the Add and Remove controls |
| `KnockoutButton` | 103 | folded into the knockout alert |
| `SaveAndOrContinueButton` | 598 | page.html draws the Next button |

## Out of scope

The port is questionnaire-only: no authentication, no MeF submission, no PDF, no data import, no state-tax handoff, no e-signature. These are the components that belong to those, listed so their absence is a decision on the record rather than a gap nobody noticed.

| Component | Declarations | Becomes |
|---|---:|---|
| `CertifyCheckbox` | 4 | out of scope: the e-signature path |
| `CollectionDataPreview` | 2 | out of scope: data import |
| `CollectionDataViewInternalLink` | 1 | out of scope: the DataView |
| `DataPreview` | 1 | out of scope: data import |
| `DownloadPDFButton` | 2 | out of scope: PDF |
| `ExitButton` | 3 | out of scope: the Direct File dashboard |
| `MefAlert` | 44 | out of scope: MeF rejection codes |
| `SaveAndOrContinueAndSetFactButton` | 14 | out of scope: submission |
| `SectionReview` | 1 | out of scope: the Checklist and DataView; the topic page is the review surface |
| `StateInfoCard` | 9 | out of scope: the state-tax handoff |
| `StateTaxReminderAlertWrapper` | 1 | out of scope: the state-tax handoff |
| `SubmitButton` | 2 | out of scope: submission |
| `TaxReturnAlert` | 54 | out of scope: MeF submission validation |

## Real gaps

Expressible in Direct File and not in this flow. Each is a thing to fix, not a thing to explain.

| Component | Declarations | Becomes |
|---|---:|---|
| `CollectionItemReference` | 13 | binds a CollectionItemReference fact, which no input type writes |

## Inline constructs with no shape in the IR

Markup met inside an authored string that the inline IR has no node for. Each one is dropped to its text, so the words survive and the markup does not — a link stops being a link, a nested list stops being a list. Counted rather than silenced, because the number is the argument for adding a node.

| Construct | Occurrences |
|---|---:|
| block `<li>` inside inline text | 39 |
| `<InternalLink>` | 16 |
| `<customerSupportLink>` | 11 |
| block `<p>` inside inline text | 5 |
| block `<ul>` inside inline text | 5 |
| `<InlinePDFButton>` | 3 |
| `<span>` | 1 |

Enum and multi-enum option labels whose markup had to be flattened to plain text, because an `<option>` carries a string: **70**.
