# Generated website
out/

# sbt
target/
project/target/
project/project/

# node
node_modules/

# Generated mirrors. Each has exactly one writer — a `make copy-*` target that runs ahead of every
# build — and none of them is a source. Edit the package, not the mirror; `make check-shared-ui`
# fails the build if the taxpert mirror drifts.
src/main/resources/direct-file/website-static/vendor/taxpert/
src/main/resources/direct-file/website-static/vendor/uswds-3.13.0/

# The Fact Graph browser bundle is the exception: it is committed, as it is in the other three
# applications here. Nothing publishes it, so without a copy in git this application has no engine
# in the browser until someone builds fact-graph and runs `make copy-fg`. Its sourcemap is ignored
# by the rule at the repository root, since it is four megabytes of debug aid rather than a runtime
# dependency.

# Metals / Bloop
.bsp/
.bloop/
.metals/
.scala-build/
metals.sbt

# JetBrains
.idea/
*.iml

# macOS
.DS_Store

# Playwright's own output: the browser downloads live in the shared ~/.cache, but traces and the HTML
# report are per-run artifacts of `make smoke`.
/test-results/
/playwright-report/
/blob-report/



# --- Anthropic / Claude Code ---
.claude/
.claude/config.json
.claudecode/
*.claudelog
.claudemdexcludes
.claudeignore
CLAUDE.MD
SKILLS.MD

# --- Cursor ---
.cursor/
.cursor/rules/
.cursor_history/
.cursorrules.local

# --- Codeium Windsurf & Cascade ---
.windsurf/
.windsurf/rules/
.windsurf/memories/
.windsurf_history/

# --- Cline & Roo Code ---
.cline/
.roocode/
.clinerules.local

# --- GitHub Copilot & Chat ---
.copilot-history/
.github/copilot-instructions.local.md

# --- Aider (Terminal AI Pair Programmer) ---
.aider.chat.history.md
.aider.input.history
.aider.tags.cache.*

# --- Open-Hands / All-Hands (SWE-Agent variants) ---
.openhands/
.openhands_cache/
.swe-agent/
swe-agent.log

# --- General Multi-Agent Tools & Context Indexes ---
.ai-context/
.ai-index/
.llm-cache/
ai-instructions.local.md